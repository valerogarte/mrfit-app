import 'package:flutter/material.dart';

class AppColors {
  /// Fondo principal de la aplicación
  static const Color background = Color.fromARGB(255, 14, 29, 47);

  /// Fondo de las tarjetas y elementos secundarios
  static const Color cardBackground = Color.fromARGB(255, 27, 49, 72);

  /// Fondo del AppBar
  static const Color appBarBackground = Color.fromARGB(255, 60, 102, 144);

  /// Color del texto en elementos destacados o blancos
  static const Color textNormal = Color.fromARGB(255, 255, 255, 255);

  /// Color del texto principal
  static const Color textMedium = Color.fromARGB(255, 224, 224, 224);

  /// Color de acento para botones y elementos destacables
  static const Color accentColor = Color.fromARGB(255, 93, 173, 226);

  /// Color de advertencia atenuado
  static const Color mutedAdvertencia = Color.fromARGB(255, 202, 202, 41);

  /// Rojo más apagado que tira hacia el cardBackground
  static const Color mutedRed = Color.fromARGB(255, 211, 84, 0);

  /// Verde alternativo
  static const Color mutedGreen = Color.fromARGB(255, 131, 191, 150);

  /// Color plata
  static const Color mutedSilver = Color.fromARGB(255, 204, 204, 204);

  /// Color bronce
  static const Color mutedBronze = Color.fromARGB(255, 205, 127, 50);
}
